type: object
required:
  - hasNext
  - page
  - limit
  - organizations
properties:
  hasNext:
    type: boolean
  offset:
    type: integer
    format: int32
  limit:
    type: integer
    format: int32
  organizations:
    type: array
    items:
      $ref: '../schemas/Organization.yaml'
