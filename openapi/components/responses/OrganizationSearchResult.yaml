type: object
required:
  - hasNext
  - offset
  - limit
  - organizations
properties:
  hasNext:
    type: boolean
  offset:
    type: integer
    format: int32
  limit:
    type: integer
    format: int32
  results:
    type: array
    items:
      $ref: '../schemas/Organization.yaml'
